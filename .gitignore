__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
env/

# Looper data — never commit secrets or the database
data/
publisher.token
server.log

# Playwright browser cache
.playwright/

# Android — APK uploaded as GitHub Release asset; source kept local only
android/
*.apk
*.aab

# LinLoop — Linux Mint snapshot, not kept in sync; local only
LinLoop/

# Node / frontend build artefacts (if ever added)
node_modules/
dist/

# Archives
LinLoop.zip

# Claude Code session data
.claude/

# macOS / Windows desktop cruft
.DS_Store
Thumbs.db
