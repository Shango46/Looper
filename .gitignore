__pycache__/
*.py[cod]
*.pyo
*.pyd
.venv/
venv/
env/

# Looper data — never commit secrets or the database
data/
publisher.token
server.log

# Playwright browser cache
.playwright/

# Android — source tracked, build artefacts and local config excluded
android/app/build/
android/.gradle/
android/build/
android/local.properties
*.apk
*.aab

# Node / frontend build artefacts (if ever added)
node_modules/
dist/

# Archives
LinLoop.zip

# Claude Code session data
.claude/

# macOS / Windows desktop cruft
.DS_Store
Thumbs.db
